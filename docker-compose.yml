services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: promptcraft
      POSTGRES_USER: promptcraft
      POSTGRES_PASSWORD: promptcraft
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
